/*Declaration of main angular module for this appllication.
 * It is named turtleFacts and has no dependencies (hence the empty array as the second argument)
 */

(function(){
    angular.module("turtleFacts", []);
})();
